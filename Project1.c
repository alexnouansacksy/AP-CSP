#include <stdio.h>
#include <stdlib.h>
#include "Project1Helper.h"

int main() {
    int num = -1;
    /*while (num != 6) {
        num = askQuestion();
        doStuff(num);
    }*/
    char* alex;
    printf("enter text: ");
    scanf("%s", alex);
    printf("%s", alex);

}
